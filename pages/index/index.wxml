<!--index.wxml-->
<view class="container">
  <scroll-view scroll-y >
    <li wx:for="{{blogData}}" wx:for-index="idx" wx:for-item="item" bindtap="openBlog" class='title' wx:key="id" data-id="{{item.id}}" data-title="{{item.title}}">
       {{item.title}}
    </li>
  </scroll-view>
</view>